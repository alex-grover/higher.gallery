query token($token: String!) {
  token(id: $token) {
    collection {
      creatorAddress
      name
      description
      image
    }
    timestamp
    name
    description
    image
    price
    maxSupply
    endTimestamp
  }
}
